# Server
PORT=3000
NODE_ENV=production

# MongoDB
MONGODB_URI=mongodb://admin:admin123@mongodb:27017/panda-quant?authSource=admin

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# JWT
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# 管理员账号配置
ADMIN_EMAIL=admin@panda-quant.com
ADMIN_PASSWORD=PandaQuant@2024

# Database Configuration
DB_PORT=27017
DB_USER=admin
DB_PASSWORD=admin123
DB_NAME=panda-quant

# Redis Configuration
REDIS_PASSWORD=$(openssl rand -base64 32)

# API 配置
USER_API_PORT=3000
ADMIN_API_PORT=3001

# 前端配置
REACT_APP_API_URL=https://api.pandatrade.space
REACT_APP_ADMIN_API_URL=https://admin-api.pandatrade.space
REACT_APP_ENV=production

# JWT Configuration
JWT_REFRESH_EXPIRES_IN=7d

# 邮件配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=pandaspace0001@gmail.com
SMTP_PASS=msdo sfko szam bzqq
SMTP_FROM=pandaspace0001@gmail.com
FRONTEND_URL=https://pandatrade.space

# 环境配置
NODE_ENV=production

# API 限制
API_RATE_LIMIT=100
API_RATE_LIMIT_WINDOW=900000

# 监控配置
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_PASSWORD=$(openssl rand -base64 32)
ALERTMANAGER_PORT=9093

# 安全配置
PASSWORD_MIN_LENGTH=12
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=true
PASSWORD_REQUIRE_SYMBOLS=true
MAX_LOGIN_ATTEMPTS=5
LOGIN_LOCKOUT_MINUTES=30
SESSION_SECRET=$(openssl rand -base64 64)

# 缓存配置
CACHE_TTL=3600

# 日志配置
LOG_LEVEL=info
LOG_FORMAT=json
LOG_RETENTION_DAYS=30

# 数据库连接
REDIS_URI=redis://:${REDIS_PASSWORD}@redis:6379

# 前端URL
ADMIN_URL=https://admin.pandatrade.space

# API URLs
USER_API_URL=https://api.pandatrade.space
ADMIN_API_URL=https://admin-api.pandatrade.space

# 备份配置
BACKUP_DIR=/backups
BACKUP_RETENTION_DAYS=30
BACKUP_SCHEDULE="0 0 * * *"

# SSL配置
SSL_CERT_PATH=/etc/letsencrypt/live/pandatrade.space/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/pandatrade.space/privkey.pem
SSL_DHPARAM_PATH=/etc/letsencrypt/ssl-dhparams.pem 